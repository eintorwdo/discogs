0.0.App provided with a band name as an argument, searches for other bands where at least 2 bandmates played together.
 0.1.School project that uses Discogs API. To make it work you need to set up your own KEY and SECRET enviroment variables in .env file
1.npm install to install required modules
2.To run: 'node app.js <band_name>'
3.Default band name - Budka Suflera ;)
